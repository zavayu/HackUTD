
  # AI-Powered Product Management App

  This is a code bundle for AI-Powered Product Management App. 
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  